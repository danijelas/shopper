<%= form_for([@list, @item], remote: true) do |f| %>
  <div class="modal-header">
    <h4 class="modal-title" id="editItemModalLabel" data-item="<%= @item.id %>"><strong><%= @item.name %> (review/edit data cica)</strong></h4>
  </div>
  <div class="modal-body">
    <div class="form-group">
      <%= f.hidden_field :category_id %>
      <%= f.hidden_field :done %>
      <div class="form-group">
        <label for="item-name" class="control-label">Name</label>
        <%= f.text_field :name, id: "item-name" %>
      </div>
      <div class="form-group">
        <label for="item-qty" class="control-label">Quantity</label>
        <%= f.text_field :qty, id: "item-qty" %>
      </div>
      <div class="form-group">
        <label for="item-unit" class="control-label">Unit</label>
        <%= f.select :unit, Item::UNIT_TYPES, prompt: 'Select Unit', id: "item-unit" %>
      </div>
      <div class="form-group">
        <label for="item-price" class="control-label">Price/Unit</label>
        <%= f.text_field :price, id: "item-price" %>
      </div>
    </div>
  </div>
  <div class="modal-footer">
    <button id="closeModal" type="button" class="btn btn-default" data-dismiss="modal">Cancel</button>
    <input id="updateModal" type="submit" class="btn btn-primary" value="Update">
  </div>
<% end -%>